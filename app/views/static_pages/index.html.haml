.row
  - if user_signed_in?
    .col-md-12
      %h1= "Hello, #{current_user}!"
  - else
    .col-md-4
      .row.well
        = render 'shared/signin_form'
      .row.well
        = render 'shared/signup_form'
    .col-md-8
      = link_to "On GitHub", 'https://github.com/fatbulat'
